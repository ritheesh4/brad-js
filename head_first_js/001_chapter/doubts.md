1.  setTimeout(wakeUpUser, 1000);
      function wakeUpUser() {
        alert("Are you going to stare forever?");
      }

  -If I change the above function into arrow function, It does not give the output. Why?